spring:
  application:
    name: schedule2

  datasource:
    url: jdbc:mysql://localhost:3306/schedule?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 12345678

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true
    properties:
        hibernate:
          format_sql: true

